title: LLMs, Applications and Agents
type: lesson
section: llms-applications-and-agents
# Retrieval first, then the agentic stack built on top of it — the order
# course.yaml declares. This section is TRANSITIONAL: the layer plan harvests
# both sub-areas into Practitioner Workflows at W4 and deletes the folder, so
# this declaration is expected to retire with it.
page-order:
  - rag-and-knowledge-systems
  - agentic-ai
